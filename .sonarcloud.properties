# SonarCloud automatic analysis settings.
# Keep non-application workspaces and deployment assets out of analysis to avoid
# separate-workspace TypeScript resolution noise and to focus the scan on the
# main app code.
sonar.exclusions=docs/**,charts/**,dev/**,deploy/**
